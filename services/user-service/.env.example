NODE_ENV=development
PORT=3002

# Must match the value in gateway and auth-service
INTERNAL_SECRET=your-internal-secret-min-32-characters!!

# PostgreSQL — users_db
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=users_db
