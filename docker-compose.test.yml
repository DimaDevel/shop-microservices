# Overlay for e2e testing: exposes the gateway directly on port 3000
# so tests bypass Nginx rate limiting.
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.test.yml up -d
#   API_URL=http://localhost:3000 npm run test:e2e
services:
  gateway:
    ports:
      - "3000:3000"
