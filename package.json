{
  "name": "nest-gateway",
  "private": true,
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --names \"gateway,auth,users\" --prefix-colors \"cyan,yellow,green\" \"npm run start:dev --prefix services/gateway\" \"npm run start:dev --prefix services/auth-service\" \"npm run start:dev --prefix services/user-service\"",
    "build": "npm run build --workspaces --if-present",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
