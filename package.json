{
  "name": "nest-gateway",
  "private": true,
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --names \"gateway,auth,users,products,orders,payments,pdf,notifications\" --prefix-colors \"cyan,yellow,green,magenta,blue,red,white,gray\" \"npm run start:dev --prefix services/gateway\" \"npm run start:dev --prefix services/auth-service\" \"npm run start:dev --prefix services/user-service\" \"npm run start:dev --prefix services/product-service\" \"npm run start:dev --prefix services/order-service\" \"npm run start:dev --prefix services/payment-service\" \"npm run start:dev --prefix services/pdf-service\" \"npm run start:dev --prefix services/notification-service\"",
    "build": "npm run build --workspaces --if-present",
    "lint": "eslint \"{services,packages}/**/*.ts\"",
    "lint:fix": "eslint \"{services,packages}/**/*.ts\" --fix",
    "format": "prettier --write \"{services,packages}/**/*.ts\"",
    "format:check": "prettier --check \"{services,packages}/**/*.ts\"",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "eslint": "^10.4.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.3",
    "typescript-eslint": "^8.60.0"
  }
}
